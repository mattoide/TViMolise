import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.matto.tvimolise',
  appName: 'TViMolise',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
